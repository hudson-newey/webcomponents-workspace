{
  "name": "@hudson-newey/web-components",
  "version": "1.1.1",
  "description": "My web components",
  "types": "@types/",
  "scripts": {
    "build": "tsc",
    "publish:components": "tsc && pnpm version patch && npm publish --access public"
  },
  "keywords": [
    "web components"
  ],
  "dependencies": {
    "lit": "^3.1.2"
  }
}
